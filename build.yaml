# Replace this on each keyboard branch.
# Each entry is a (board, shield) pair built by .github/workflows/build.yml.
# See https://zmk.dev/docs/development/local-toolchain/build-flags for cmake-args/snippet options.
---
include: []
  # - board: nice_nano_v2
  #   shield: <keyboard>_left nice_view_adapter nice_view
  # - board: nice_nano_v2
  #   shield: <keyboard>_right nice_view_adapter nice_view
  # - board: nice_nano_v2
  #   shield: settings_reset
